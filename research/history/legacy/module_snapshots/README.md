# Pre-refactor module snapshots

Relocated 2026-08-10 from `pps_qj/`. **Not deleted.**

They previously sat beside the live modules as `*.bak_{guided,spawn,prelowrank,prenewton}`,
where they shadowed live code in grep results and risked accidental import.
They are historical only and are **not authoritative**. Do not import them and do
not cite them as evidence.
