# SOURCE_REGISTER — <TASK-ID>         (Charter Stage 0, task-scoped)

Only sources LOAD-BEARING for this question. Global registry inspection is not
required and is not attempted. **Freezes at `stage_1_problem`** — later
inspections are appended to the section at the bottom, not folded in above.

**This file is the task-level authority for source inspection achieved during
this run.** A source inspected here is `task-verified`: admissible within this
task, including by the red team, and recorded machine-readably in
`TASK_EVIDENCE.yaml`. It does **not** become canonical — that needs the human
merge gate. The four tiers are: canonical (`research/state/**`), task-verified
(here), provenance (manuscripts, `theory/**`, `audit/**`, history), and
conjecture.

## Load-bearing sources

| SRC-ID | citation | inspection_level | what was actually read | date | supports_exactly | does NOT establish |
|---|---|---|---|---|---|---|

inspection_level: fully_inspected | relevant_sections | abstract_only | not_inspected
Only the first two may be used as support (validator E20).

## Sources inspected during this task  →  TASK-VERIFIED

Appended as the run proceeds; this section is exempt from the Stage 1 freeze.
Each entry also gets a `TV-*` record in `TASK_EVIDENCE.yaml`.

| TV id | SRC-ID | what was actually read | what it establishes | what it does NOT | proposed canonical change |
|---|---|---|---|---|---|

**Admissible in this task. Not canonical.** The proposed change goes to
`proposed/` and waits for the human.

## Sources that could not be inspected
| SRC-ID | why | does it block a defensible decision? |
|---|---|---|

If any row answers "yes", the recommendation is **Infrastructure first**.

## Search log
Terms tried, terms that returned nothing, locations searched.

## Code and data provenance
Which script produced the numbers in scope; does it still exist; does the stored
observable definition match the formula relied on.
