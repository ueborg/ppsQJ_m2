# scratch/ — disposable working files

Plots, intermediate CSVs, exploratory scripts. Never cited as evidence, never
promoted to `research/state/**` without going through `proposed/` and the human
gate. Safe to delete after the task closes.

The numerics agent writes here and nowhere else.
